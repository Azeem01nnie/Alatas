# Supabase (full-online backend)
VITE_SUPABASE_URL=https://lspqhbbgvnpygiohcskg.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_KhI7gWyvREE0RMfm5l0vrA_onsoQuIu
VITE_USE_SUPABASE=true

# OR/CR scan uses Supabase Edge Function `scan-orcr`.
# Do NOT put API keys here — set them as Supabase function secrets:
#   GOOGLE_SERVICE_ACCOUNT_JSON  (Document AI — full service account JSON)
#   DOCUMENT_AI_PROCESSOR        (projects/.../locations/.../processors/...)
#   DOCUMENT_AI_LOCATION         (optional, default us)
#   GEMINI_API_KEY               (optional — desk Gemini engine)
# (see supabase/functions/scan-orcr/README.md)
# Legacy Render sync — keep false when using Supabase
VITE_CLOUD_SYNC_ENABLED=false
VITE_RENDER_API_URL=
