{
  "name": "alatas",
  "private": true,
  "version": "1.1.0",
  "description": "Alatas Car Rental Services — web app and Windows desktop (Electron)",
  "main": "electron/main.cjs",
  "author": "Alatas Car Rental Services",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:app": "npm run dev --prefix frontend",
    "dev:legacy-api": "npm run dev --prefix backend",
    "install:all": "npm install && npm install --prefix frontend",
    "start": "npm run dev",
    "pwa:preview": "npm run build --prefix frontend && npm run preview --prefix frontend",
    "electron:dev": "concurrently -k -n frontend,electron -c cyan,green \"npm run dev --prefix frontend\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "icons": "node electron/make-icons.cjs",
    "electron:build": "npm run icons && npm run build --prefix frontend && electron-builder --win"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.1",
    "concurrently": "^9.2.1",
    "electron": "^37.2.0",
    "electron-builder": "^26.0.12",
    "jimp": "^1.6.1",
    "png-to-ico": "^3.0.2",
    "wait-on": "^8.0.3"
  }
}
